namespace OpenKustoExplorer.Application.Sessions;

/// <summary>
/// Identifies a chain endpoint role.
/// </summary>
public enum KustoChainEndpointRole
{
    /// <summary>The chain starts at the selected value or row.</summary>
    Start,

    /// <summary>The chain ends at the selected value or row.</summary>
    End,
}
